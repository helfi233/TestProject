using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Print : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {   //вывод в консоль привет мир 
        print ("Hello,World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
